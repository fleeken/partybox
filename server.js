import express from 'express';
import http from 'node:http';
import { Server } from 'socket.io';
import { randomInt } from 'node:crypto';
const app=express();const server=http.createServer(app);const io=new Server(server);const PORT=process.env.PORT||3000;const rooms=new Map();
const colors=['#ff4757','#2ed573','#1e90ff','#ffa502','#a55eea','#00d2d3','#ff6b81','#eccc68'];const avatars=['😎','🤓','🥳','🤠','😜','😁','😮','😏'];const games=['Stoppa på 10.00','Quiz Battle','Färgfeed','Bomben','Mini-race','Copycat','Rött ljus / grönt ljus','Prickskytten','Chicken','Växla!','Hal is','Hajen kommer','Väggen','Kanonduell','Helikoptern','Radiobilsfotboll'];const playableGames=['Stoppa på 10.00'];
app.use(express.static('public'));app.get('/health',(_,res)=>res.json({ok:true,rooms:rooms.size}));function code(){const chars='ABCDEFGHJKLMNPQRSTUVWXYZ23456789';let value='';do{value=Array.from({length:5},()=>chars[randomInt(chars.length)]).join('')}while(rooms.has(value));return value}function findPlayer(room,id){return room.players.find(p=>p.id===id)}function publicRoom(room){return{code:room.code,phase:room.phase,players:room.players.map(p=>({name:p.name,color:p.color,avatar:p.avatar,score:p.score,voted:p.voted,ready:p.ready,stopped:Boolean(room.game?.stops?.has(p.id))})),chat:room.chat||[],voteOptions:room.voteOptions,gameOptions:games.map(name=>({name,playable:playableGames.includes(name)})),selectedGame:room.selectedGame,game:room.game?{name:room.game.name,status:room.game.status,startsAt:room.game.startsAt,revealUntil:room.game.revealUntil,stoppedCount:room.game.stops.size,results:room.game.results||[]}:null}}function emitRoom(room){io.to(room.code).emit('room:update',publicRoom(room))}function beginPractice(room,gameName){room.selectedGame=gameName;room.phase='practice';room.players.forEach(p=>p.ready=false);room.game={name:gameName,status:'practice',startsAt:Date.now(),revealUntil:0,stops:new Map(),results:[]};emitRoom(room)}function finishTen(room){if(!room.game||room.game.name!=='Stoppa på 10.00'||room.game.status==='results')return;if(room.game.finishTimer)clearTimeout(room.game.finishTimer);const results=room.players.map(player=>{const elapsed=room.game.stops.get(player.id),validElapsed=elapsed??15000,diff=Math.abs(validElapsed-10000),points=Math.max(0,Math.round(1000-diff*.2));player.score+=points;return{name:player.name,color:player.color,avatar:player.avatar,elapsed:validElapsed,diff,points}}).sort((a,b)=>a.diff-b.diff);room.game.status='results';room.game.results=results;room.phase='results';emitRoom(room)}function startRealTen(room){const now=Date.now();room.phase='game';room.players.forEach(p=>p.ready=false);room.game={name:'Stoppa på 10.00',status:'countdown',startsAt:now+3500,revealUntil:now+5500,stops:new Map(),results:[]};emitRoom(room);setTimeout(()=>{if(rooms.has(room.code)&&room.game?.name==='Stoppa på 10.00'){room.game.status='running';emitRoom(room)}},3500);room.game.finishTimer=setTimeout(()=>finishTen(room),18500)}
io.on('connection',socket=>{socket.on('host:create',(_,reply=()=>{})=>{const roomCode=code(),room={code:roomCode,hostId:socket.id,phase:'lobby',players:[],chat:[],voteOptions:[],votes:new Map(),selectedGame:null,game:null};rooms.set(roomCode,room);socket.join(roomCode);socket.data.roomCode=roomCode;socket.data.role='host';reply({ok:true,room:publicRoom(room)});emitRoom(room)});socket.on('player:join',({roomCode,name,avatar},reply=()=>{})=>{const key=String(roomCode||'').trim().toUpperCase(),room=rooms.get(key);if(!room)return reply({ok:false,error:'Lobbyn finns inte.'});if(room.players.length>=8)return reply({ok:false,error:'Lobbyn är full.'});if(!['lobby','voting','selected','choosing'].includes(room.phase))return reply({ok:false,error:'En runda pågår. Vänta till nästa spel.'});const cleanName=String(name||'Spelare').trim().slice(0,18)||'Spelare',avatarIndex=Math.max(0,Math.min(7,Number.parseInt(avatar,10)||0)),player={id:socket.id,name:cleanName,color:colors[room.players.length],avatar:avatars[avatarIndex],score:0,voted:false,ready:false};room.players.push(player);socket.join(key);socket.data.roomCode=key;socket.data.role='player';reply({ok:true,player:{name:player.name,color:player.color,avatar:player.avatar},room:publicRoom(room)});emitRoom(room)});socket.on('chat:send',({text},reply=()=>{})=>{const room=rooms.get(socket.data.roomCode),player=room&&findPlayer(room,socket.id);if(!room||!player||!['lobby','results'].includes(room.phase))return reply({ok:false});const clean=String(text||'').trim().slice(0,160);if(!clean)return reply({ok:false});room.chat.push({name:player.name,avatar:player.avatar,color:player.color,text:clean,at:Date.now()});if(room.chat.length>40)room.chat=room.chat.slice(-40);emitRoom(room);reply({ok:true})});socket.on('player:lobbyReady',(_,reply=()=>{})=>{const room=rooms.get(socket.data.roomCode),player=room&&findPlayer(room,socket.id);if(!room||!player||room.phase!=='lobby')return reply({ok:false});player.ready=!player.ready;emitRoom(room);reply({ok:true,ready:player.ready})});socket.on('host:startVote',(_,reply=()=>{})=>{const room=rooms.get(socket.data.roomCode);if(!room||room.hostId!==socket.id||!room.players.length)return reply({ok:false});room.phase='voting';room.game=null;room.selectedGame=null;room.votes.clear();room.players.forEach(p=>{p.voted=false;p.ready=false});room.voteOptions=[...games].sort(()=>Math.random()-.5).slice(0,3);emitRoom(room);reply({ok:true})});socket.on('host:startChoose',(_,reply=()=>{})=>{const room=rooms.get(socket.data.roomCode);if(!room||room.hostId!==socket.id||!room.players.length)return reply({ok:false});room.phase='choosing';room.votes.clear();room.players.forEach(p=>p.voted=false);emitRoom(room);reply({ok:true})});socket.on('host:chooseGame',({game},reply=()=>{})=>{const room=rooms.get(socket.data.roomCode);if(!room||room.hostId!==socket.id||room.phase!=='choosing'||!games.includes(game))return reply({ok:false});if(!playableGames.includes(game))return reply({ok:false,error:'Det spelet är inte byggt ännu.'});room.selectedGame=game;room.phase='selected';emitRoom(room);reply({ok:true})});socket.on('player:vote',({game},reply=()=>{})=>{const room=rooms.get(socket.data.roomCode),player=room&&findPlayer(room,socket.id);if(!room||!player||room.phase!=='voting'||!room.voteOptions.includes(game))return;room.votes.set(socket.id,game);player.voted=true;emitRoom(room);reply({ok:true});if(room.players.length&&room.votes.size===room.players.length){const counts=Object.fromEntries(room.voteOptions.map(g=>[g,0]));for(const vote of room.votes.values())counts[vote]++;const max=Math.max(...Object.values(counts)),tied=room.voteOptions.filter(g=>counts[g]===max),winner=tied[randomInt(tied.length)];room.selectedGame=winner;room.phase='selected';io.to(room.code).emit('vote:result',{winner,counts});emitRoom(room)}});socket.on('host:startSelected',(_,reply=()=>{})=>{const room=rooms.get(socket.data.roomCode);if(!room||room.hostId!==socket.id)return reply({ok:false});if(room.selectedGame!=='Stoppa på 10.00')return reply({ok:false,error:'Det spelet är inte byggt ännu.'});beginPractice(room,room.selectedGame);reply({ok:true})});socket.on('host:startTen',(_,reply=()=>{})=>{const room=rooms.get(socket.data.roomCode);if(!room||room.hostId!==socket.id||!room.players.length)return reply({ok:false});beginPractice(room,'Stoppa på 10.00');reply({ok:true})});socket.on('player:ready',(_,reply=()=>{})=>{const room=rooms.get(socket.data.roomCode),player=room&&findPlayer(room,socket.id);if(!room||!player||room.phase!=='practice')return reply({ok:false});player.ready=true;emitRoom(room);reply({ok:true});if(room.players.length&&room.players.every(p=>p.ready)&&room.selectedGame==='Stoppa på 10.00')startRealTen(room)});socket.on('player:stopTen',(_,reply=()=>{})=>{const room=rooms.get(socket.data.roomCode),player=room&&findPlayer(room,socket.id);if(!room||!player||room.phase!=='game'||room.game?.name!=='Stoppa på 10.00')return reply({ok:false});if(room.game.stops.has(socket.id))return reply({ok:false});const elapsed=Date.now()-room.game.startsAt;if(elapsed<0)return reply({ok:false,error:'För tidigt!'});room.game.stops.set(socket.id,Math.min(elapsed,15000));reply({ok:true});emitRoom(room);if(room.game.stops.size===room.players.length)finishTen(room)});socket.on('disconnect',()=>{const room=rooms.get(socket.data.roomCode);if(!room)return;if(room.hostId===socket.id){if(room.game?.finishTimer)clearTimeout(room.game.finishTimer);io.to(room.code).emit('room:closed');rooms.delete(room.code);return}room.players=room.players.filter(p=>p.id!==socket.id);room.votes.delete(socket.id);room.game?.stops?.delete(socket.id);if(room.phase==='practice'&&room.players.length&&room.players.every(p=>p.ready)&&room.selectedGame==='Stoppa på 10.00')startRealTen(room);else if(room.phase==='game'&&room.players.length&&room.game?.stops.size===room.players.length)finishTen(room);else emitRoom(room)})});server.listen(PORT,'0.0.0.0',()=>console.log(`Partybox running on :${PORT}`));